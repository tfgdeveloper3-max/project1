import { motion, type Variants } from "motion/react";
import {
    Bell,
    WifiOff,
    Fingerprint,
    Store,
    RefreshCw,
    Gauge,
} from "lucide-react";

const APP_CAPABILITIES = [
    {
        icon: Bell,
        title: "Push Notifications",
        description: "Re-engage users with targeted, real-time push notifications on both platforms.",
    },
    {
        icon: WifiOff,
        title: "Offline Mode Support",
        description: "Core features keep working without a connection, syncing automatically when back online.",
    },
    {
        icon: Fingerprint,
        title: "Biometric Authentication",
        description: "Face ID and fingerprint login for a secure, frictionless sign-in experience.",
    },
    {
        icon: Store,
        title: "App Store Optimization",
        description: "Listings, screenshots, and metadata optimized to get your app discovered and downloaded.",
    },
    {
        icon: RefreshCw,
        title: "Over-The-Air Updates",
        description: "Ship fixes and new features without waiting on lengthy app store review cycles.",
    },
    {
        icon: Gauge,
        title: "Performance Monitoring",
        description: "Real-time crash reporting and analytics so issues get caught before users complain.",
    },
];

const container: Variants = {
    hidden: {},
    visible: { transition: { staggerChildren: 0.1, delayChildren: 0.05 } },
};

const cardItem: Variants = {
    hidden: { opacity: 0, y: 30 },
    visible: {
        opacity: 1,
        y: 0,
        transition: { duration: 0.7, ease: [0.22, 1, 0.36, 1] },
    },
};

const headerItem: Variants = {
    hidden: { opacity: 0, y: 24 },
    visible: {
        opacity: 1,
        y: 0,
        transition: { duration: 0.7, ease: [0.22, 1, 0.36, 1] },
    },
};

export default function MobileAppsCapabilitiesSection() {
    return (
        <section className="w-full bg-white px-6 py-20 font-atyp md:px-12 lg:py-28">
            <motion.div
                className="mx-auto max-w-3xl text-center"
                initial="hidden"
                whileInView="visible"
                viewport={{ once: false, amount: 0.3 }}
                variants={container}
            >
                <motion.span
                    variants={headerItem}
                    className="mb-6 inline-flex w-fit items-center rounded-full bg-neutral-200/70 px-5 py-2 text-sm text-neutral-700"
                >
                    App Capabilities
                </motion.span>

                <motion.h2
                    variants={headerItem}
                    className="text-[32px] font-light leading-[1.2] text-neutral-900 sm:text-[38px] lg:text-[42px]"
                >
                    Every Feature Your{" "}
                    <span className="font-semibold text-brand">App</span> Needs
                </motion.h2>

                <motion.p
                    variants={headerItem}
                    className="mx-auto mt-5 max-w-xl text-[15px] leading-relaxed text-neutral-500"
                >
                    From Offline Support To Biometric Login, We Build The Features That
                    Make Your App Feel Complete And Ready For Real Users.
                </motion.p>
            </motion.div>

            <motion.div
                className="mx-auto mt-14 grid max-w-6xl grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3"
                initial="hidden"
                whileInView="visible"
                viewport={{ once: false, amount: 0.2 }}
                variants={container}
            >
                {APP_CAPABILITIES.map((feature) => {
                    const Icon = feature.icon;
                    return (
                        <motion.div
                            key={feature.title}
                            variants={cardItem}
                            className="group rounded-[22px] bg-[#f5f5f4] p-7 transition-colors duration-500 hover:bg-brand"
                        >
                            <div className="flex h-14 w-14 items-center justify-center rounded-full bg-brand text-white transition-colors duration-500 group-hover:bg-white group-hover:text-brand">
                                <Icon className="h-6 w-6" strokeWidth={1.75} />
                            </div>

                            <h3 className="mt-6 text-lg font-semibold text-neutral-900 transition-colors duration-500 group-hover:text-white">
                                {feature.title}
                            </h3>
                            <p className="mt-2 text-sm leading-relaxed text-neutral-500 transition-colors duration-500 group-hover:text-white/85">
                                {feature.description}
                            </p>
                        </motion.div>
                    );
                })}
            </motion.div>
        </section>
    );
}